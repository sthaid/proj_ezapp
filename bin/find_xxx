#!/bin/bash

top=$(git rev-parse --show-toplevel)
if [ $? -ne 0 ]; then
    echo "ERROR failed to get top repo directory"
    exit 1
fi
cd $top

git ls-files | xargs grep xxx |& \
egrep -v "^src/libmp3lame|^src/cJSON|^todo|^devel|xxx.xxx.xxx.xxx|mipmap-xxxhdpi|binary file matches|^bin/find_xxx"
